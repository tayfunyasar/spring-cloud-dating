server:
  port: 8888
  
management:
  security:
    enabled: false  
                
spring:
  application:
    name: config-server
  cloud:
     config:
       server:
           git:
              uri: configs
              

logging:
  level:
    org.springframework.cloud: 'DEBUG'